import { PlaceholderScreen } from "../components/PlaceholderScreen";

/** Shell da aba Notificações — API, WebSocket, push, badge real e leitura ficam para depois (Fase 4, item 18). */
export function NotificationsScreen() {
  return <PlaceholderScreen title="Notificações" description="Aqui aparecerão suas notificações." />;
}
