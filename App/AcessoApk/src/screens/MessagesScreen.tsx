import { PlaceholderScreen } from "../components/PlaceholderScreen";

/** Shell da aba Mensagens — o chat em si é de uma fase específica futura (Fase 4, item 17). */
export function MessagesScreen() {
  return <PlaceholderScreen title="Mensagens" description="Aqui ficarão suas conversas." />;
}
