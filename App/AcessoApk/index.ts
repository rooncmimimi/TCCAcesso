import { registerRootComponent } from 'expo';

import App from './App';

// `registerRootComponent` registra o `App` como componente principal e prepara o ambiente tanto no
// Expo Go quanto num build nativo.
registerRootComponent(App);
